# CureNet AI (enhanced)

A small Flask app that generates personalized rehabilitation plans using a rules-based engine.

## Requirements
- Python 3.10+ recommended
- pip

## Setup (local)
1. Clone or copy the project folder.
2. Create and activate a virtual environment (optional but recommended):

```bash
python -m venv venv
# mac / linux
source venv/bin/activate
# windows (powershell)
venv\Scripts\Activate.ps1
